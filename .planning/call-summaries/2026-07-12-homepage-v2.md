# Call Summary — 首頁 v2 重構（8plus Design System 匯入）

**時間戳記**：2026-07-12
**分支**：`feature/claude/2026-07-v3`
**狀態**：主頁完成；其他內頁明天再改

---

## 目標
把 claude.ai/design「8plus Design System」專案（projectId `1b9e8395-76e9-42ea-adc8-0206b86c194f`）的 v2 UI kit（`ui_kits/8plus-app-v2/index.html`）實作進 Next.js 專案，保留既有 design system，並讓首頁視覺完整重現 kit。

## 已完成（主頁）
1. **內容模型重構** — `lib/content/home-sections.ts`
   - hero 新標題「AI 沒有魔法，只有工程／對的架構，接住你的需求」
   - About 擴充：kicker、lead、summary、能力光譜(4)、亮點(4)、技術 chips
   - 新增 Path 職涯時間軸資料
   - 區塊重編號、section IDs 移除 blog
2. **區塊重排＋移除 Journal** — 順序 hero → about → lab → path → services → booking
3. **CI 交替色場系統** — `styles/globals.css`（`.bg-blue/.bg-orange/.bg-dark` + accent 翻轉 + `.noise-field` + `.gradient-border-card`）
   - hero 藍 → about 橘 → lab 藍 → path 橘 → services 藍 → booking 深夜
4. **可切換主視覺 hero**
   - `components/home/hero/hero-backdrops.tsx`（21 個 canvas/SVG 動畫，逐字移植 HeroBackdropsV2 + HeroBackdrops2V2）
   - `components/home/hero/hero-v2.tsx`（combo/flow/logo/lines 內建 + 右下切換器 + corner/flat 版面）
   - **每次載入隨機挑一個動畫**（25 選 1）；切換器手動選當次有效
5. **Lab 編輯式卡片** — `section-lab.tsx`，縮圖來自 `public/og/labs/<id>/web.png`（remap：crm-series→crm、power-bi→powerbi）
6. 新增 `components/home/sections/section-path.tsx`、`section-about.tsx` 重寫、progress-nav 更新

## 驗證
- `pnpm typecheck` ✅ `pnpm build` ✅（`/` 靜態預渲染）
- Hero 主視覺實截：combo、iso、flow、sphere 皆正常
- 隨機載入實測：連續刷新得到不同動畫
- 交替色場 computed style 確認：About/Path 橘（accent→藍）、Lab/Services 藍（accent→橘）、Booking 深夜

## 變動檔案
- 新增：`.claude/launch.json`、`components/home/hero/hero-backdrops.tsx`、`components/home/hero/hero-v2.tsx`、`components/home/sections/section-path.tsx`、`public/og/labs/powerbi/powerbi.jpeg`
- 修改：`lib/content/home-sections.ts`、`styles/globals.css`、`components/home/home-scroll-root.tsx`、`home-section-progress-nav.tsx`、`sections/section-about.tsx`、`section-lab.tsx`、`section-services.tsx`、`section-booking.tsx`

## 待辦（明天）
- **其他內頁全部要改**：`/about`、`/lab`(+`/lab/[slug]`)、`/path`、`/services`、`/booking`、`/projects`、`/contact`、`/blog` 等，套用 v2 CI（交替色場、DS 元件、與首頁一致的視覺語言）。**目前只有主頁算完成。**

## 未決／取捨
- 尚未 commit（都在 feature 分支）
- Booking 保留真 cal.com（放深夜場），非 kit 假時段選擇器
- 全站 header 仍有「博客」連結（全站導覽決策，未動）
- `components/home/sections/section-hero.tsx`（舊 handshake hero）已無用、保留檔案未刪
