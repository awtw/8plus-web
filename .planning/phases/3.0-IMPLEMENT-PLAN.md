# Phase 3.0 — 實作計畫

*建立：2026-06-30 CST*  
*依據：`docs/MOTION_DESIGN_SPEC.md`（LOCKED）、`.planning/MOTION_CHECKPOINT.md`*

---

## W0 — 基礎設施（先做）

**範圍**
- [x] `pnpm add gsap`（ScrollTrigger）
- [x] `styles/globals.css` — dark cinematic 為 `:root` 預設
- [x] `app/layout.tsx` — `className="dark"`；`colorScheme: dark`
- [x] 移除 `ThemeToggle`（`site-header`、`mobile-nav`）
- [x] `components/motion/` — `useReducedMotion`、`MotionReveal`、`ScrollChapter`
- [x] `lib/content/home-scroll.ts` — Ch0–Ch5 中英文案
- [x] `lib/motion/gsap-client.ts` — GSAP 註冊

**不觸碰**：首頁舊 Hero 整批替換（留 W1）

**驗收**
- `pnpm typecheck && pnpm build`
- 全站 dark；無 light 切換 UI
- GSAP 可 import

---

## W1 — 首頁原型 Ch0 + Ch1

**範圍**
- [x] `components/home/scroll-story/`
- [x] Ch0 Logo 組裝（SVG + GSAP）
- [x] Ch1 Story pin + 4 beat scrub
- [x] 章節 progress nav（00–01）
- [x] 首頁 `ScrollStoryRoot` 接入

**驗收**
- 桌機 scroll pin 流暢
- `prefers-reduced-motion` 靜態 fallback
- 375px 無橫向溢出

---

## W2 — 首頁 Ch2–Ch5

**範圍**
- [ ] Lab — blog 預覽 stagger
- [ ] Projects — case study + metrics scrub
- [ ] Services — A/B/C pin 模組
- [ ] Connect — booking + `LineContactCard`
- [ ] i18n 全章節 en/zh-TW

**驗收**
- 完整 6 章 narrative
- 中英切換文案正確

---

## W3 — `/path` + 內頁

**範圍**
- [ ] `/path` — WRK 式水平職涯 timeline（ScrollTrigger horizontal）
- [ ] about / services / contact — `MotionReveal` 輕量套用

**驗收**
- path timeline 可 swipe/scroll
- 內頁不搶首頁戲

---

## W4 — 品質

**範圍**
- [ ] mobile parallax 降級
- [ ] `docs/DESIGN_SYSTEM_SPEC.md`
- [ ] 更新 `DESIGN_SYSTEM_COMPLIANCE_REPORT.md`
- [ ] Lighthouse / bundle 檢查

---

## 依賴

```
W0 → W1 → W2
         ↘ W3（可與 W2 尾端並行）
W2 + W3 → W4
```

---

*下一步：執行 W0。*
