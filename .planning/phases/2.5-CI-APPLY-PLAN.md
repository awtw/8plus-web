# Phase 2.5 — CI 分階套用計畫

*建立：2026-06-30 CST*  
*依據：`docs/CI_IDENTITY_SPEC.md`、`.planning/CI_CHECKPOINT.md`*

---

## 目標

依 CI 規格**漸進套用**，每階段可獨立驗收與回滾，避免「整站重做」。

---

## Phase 2.5-P1 — 基礎原子（當前）

**範圍**
- [x] `CI_CHECKPOINT.md` 記憶點
- [ ] `text-h1` … `text-h6` 語意 class
- [ ] `Logo` mono 改 `--logo-mark`
- [ ] `LuminaShell` 僅包 `HeroSection`
- [ ] 新增 `ci-*` section 樣式（Cohere 白底）
- [ ] 首頁 Hero 改用 `text-h*`；下區首批 `aurora→ci`

**不觸碰**：內頁、Share、design-lab 粒子模式

**驗收**：Hero 暗 / 下區亮；H 層級可讀

---

## Phase 2.5-P2 — 首頁完整收斂

- 首頁全部 `aurora-*` → `ci-*`
- Pillar 標題 → `text-h4`；區段標題 → `text-h2`
- CTA 統一 `brand-button-primary` / ghost
- 移除首頁殘留 `animate-aurora`

---

## Phase 2.5-P3 — 文案對齊定位

- `lib/i18n.ts`：`home.services.*`、`about.content` 對齊 pillars 敘事
- `lib/content/about.ts` 若存在則同步
- 中英一致語氣

---

## Phase 2.5-P4 — 內頁標題系統

- `/services`、`/process`、`/pricing`、`/contact` 標題 `text-h2`
- `prose` 內 H3–H5 對照表

---

## Phase 2.5-P5 — 合規與文件

- 更新 `docs/DESIGN_SYSTEM_COMPLIANCE_REPORT.md`
- 更新 `.planning/STATE.md`
- 可選：light token 收斂至 canonical Cohere hex

---

## 依賴圖

```
P0 記憶點
 └─ P1 原子 + Hero 分界
     └─ P2 首頁
         └─ P3 文案
             └─ P4 內頁
                 └─ P5 審計
```

---

## 風險與緩解

| 風險 | 緩解 |
|------|------|
| 暗色 Hero / 白底斷層突兀 | Hero 底 `border-bottom` + 下區 `--bg` 純白 |
| CSS 膨脹（aurora+ci+cinema） | P2 後刪未用 aurora 首頁引用；長期清 dead CSS |
| 又不滿意 Hero | 只改 `hero-logo-cinema` + lab；不動 ci 系統 |
